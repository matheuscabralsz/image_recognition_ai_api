# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Model to use (gpt-4o, gpt-4o-mini, gpt-4-turbo, or gpt-4-vision-preview)
MODEL=gpt-4o

# Directory containing images to process
IMAGES_DIR=./images

# Output file for results
OUTPUT_FILE=./results/results.json

# Prompt to send with each image
PROMPT=Describe this image in detail, including any text, objects, people, and activities visible.

# Maximum tokens for each response
MAX_TOKENS=500

# Number of concurrent requests (be mindful of rate limits)
CONCURRENCY=5

# Delay between batches in milliseconds
BATCH_DELAY=1000
